{
    "accounts": [
        {
            "id": "test",
            "name": "Foo Bar",
            "email": "test@test.com",
            "sendmail": "true",
            "default": "true"
        }
    ],

    "compose": {
        "templates": [
            {
                "shortcut": "0",
                "description": "thanks",
                "template": "Thanks, sounds good, let's do it that way!"
            }
        ]
    }
}
